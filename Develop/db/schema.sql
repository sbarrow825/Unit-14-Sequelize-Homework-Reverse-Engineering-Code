-- if there doesn't already exist a database with the name "passport_demo" 
-- on your MySQL connection, go ahead and make it
CREATE DATABASE IF NOT EXISTS passport_demo; 

-- use the "passport_demo" database
USE passport_demo;